# 安全与非目标

本工具只做离线、有界模型分析，不扫描地址、不发送报文、不读取系统防火墙、不执行配置、不修改真实设备。不能据此承诺真实网络不中断，也不能用来认证云安全组、NAT、状态连接或路由。

- 使用前审查从实际配置到本 DSL 的映射；遗漏协议、方向、默认动作会改变语义。
- 不要将 audit 的退出码 0 理解为没有风险；需要读取每条规则的 coverage。
- diff/check 返回 1 是业务失败；2/3/4 是无有效结论，不应继续发布。
- BDD 最坏情况指数复杂度；节点预算与单操作 work 限制不是整请求耗时保证。对不可信策略使用进程级超时和隔离。
- CLI 拒绝非普通文件、超 128 KiB 文件及无效 UTF-8。只读文件，不展开脚本或执行内容。纯核心仍自行验证文本和规则界限。
- 浏览器无上传或持久化；Worker 可取消并在 30 秒终止。本地服务绑定 loopback，固定五个可读文件，不应反向代理到公网。
- 不要在公开 issue 中粘贴生产策略、凭证或客户网络信息。先构造使用文档地址的最小复现，并通过 GitHub 私密漏洞报告联系维护者（若尚未启用，先提交不含敏感内容的联系方式请求）。

支持版本：0.1.x。无专业安全认证、外部审计或生产可用性保证。依赖及 AI 使用说明见 README / THIRD_PARTY。
